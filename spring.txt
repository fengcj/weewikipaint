1) ApplicationContextInitializer 接口
	ApplicationContextInitializer是Spring框架原有的概念,
        这个类的主要目的就是在ConfigurableApplicationContext类型（或者子类型）的ApplicationContext做refresh之前，
	允许对ConfigurableApplicationContext的实例做进一步的设置或者处理。

